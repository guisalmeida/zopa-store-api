import styled from 'styled-components'

export const CartIconCounter = styled.sup`
  overflow: hidden;
  position: absolute;
  left: -0.75rem;
  top: -0.5rem;
  width: 1.25rem;
  height: 1.25rem;
  border-radius: 50%;
  background: #cf3838;
  color: #fff;
  line-height: 1.25rem;
  text-align: center;
  padding: 0;
  font-size: 0.75rem;
  font-weight: bold;
  white-space: nowrap;
  z-index: 10;
`
